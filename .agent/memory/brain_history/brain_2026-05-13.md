# Antigravity (DX Agent) 🦾 — Brain Dump

## Session: 2026-05-11

### Status: Phase 10 & 10.4 Completed — NOC Pro & Modularization
- **Dashboard NOC Pro**: Implementada telemetría de alta densidad. Monitorización de hardware (CPU/RAM/Disco), red (ETH0 RX/TX), base de datos (hilos/slow queries) y servicios IA.
- **Modularización**: Secciones de Backups y Auditoría (Logs) separadas en módulos independientes con UI unificada y consistente.
- **Infraestructura de Backups**: Estabilizado el motor de backups usando `mariadb-client` en el contenedor y script `backup-db.sh` optimizado (SSL disabled, bash forzado).
- **Git Intelligence**: Extracción de metadatos (Hash/Fecha) operativa mediante configuración `--system` de `safe.directory`. Localización completa a castellano con Carbon.
- **Quick Actions**: Panel interactivo con Alpine.js para gestión rápida del servidor (Caché, Workers, DB, Mantenimiento).

### Technical Notes
- **Git Permissions**: Crucial usar `git config --system` en Docker para que `www-data` pueda ejecutar comandos de git sobre volúmenes montados.
- **Carbon Localization**: Para fechas de Git, usar `Carbon::createFromTimestamp($ts)->diffForHumans()` tras configurar el locale global a `es`.
- **Docker Paths**: Recordar que con `--project-directory .`, todas las rutas de volúmenes en el compose deben ser relativas a la raíz.
- **Backup Permissions**: Los archivos generados por `mysqldump` dentro del contenedor pueden requerir ajuste de permisos para ser gestionados vía web (IDs seguros).

### Next Steps
- **Phase 11**: Gestión de Usuarios y Roles (RBAC granular).
- **Security Audit**: Revisar middleware de autenticación ante el nuevo sistema de usuarios.
- **Integración**: Vincular los logs de auditoría con el nuevo módulo de usuarios para trazabilidad nominal.

_Firmado por: **Antigravity (DX Agent)** 🦾_
