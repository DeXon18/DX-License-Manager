# Memory Dump — DX License Manager
> Última sesión: 2026-05-08 13:55
> Rama: fix/cod-history-and-signed-upload
> Agente: Antigravity (DX Agent) 🦾

## 🧠 Contexto Activo (Active Context)
- **Fase**: 5.5 / 6 (Finalización Certificados COD y Limpieza de Infraestructura).
- **Hito principal**: El ciclo de vida de los certificados COD de Siemens ha sido estabilizado y blindado.
- **Logro técnico**: Implementación de subida directa (Auto-submit) de PDFs firmados, sustituyendo el modal fallido de Alpine.js. Mapeo de rutas privadas hacia el host Docker corregido (`storage/app/private` -> `storage/private`).
- **Limpieza**: Eliminados archivos SQL de backup antiguos y purgada la caché de vistas y configuración.

## 🏗 Arquitectura Mental (Mental Model)
- **Rutas de Almacenamiento**: El disco `private` ahora cuelga de `app/private` para garantizar que Docker lo mapee a la unidad `Z:\` del host.
- **Permisos Samba**: Las carpetas nuevas creadas por Docker (root) requieren un `chmod -R 777` recursivo en el host para ser visibles en Windows via Samba.
- **Labels COD**: Mapeo estético en Blade para `CHANGE_FULL`, `CHANGE_COMPOSITE` y `CHANGE_NODELOCKED` a nombres en castellano amigables.

## 🛑 Dead Ends (Lo que NO funciona)
- Usar `Storage::disk('private')->download()` a veces confunde al linter PHP; mejor usar `response()->download(Storage::disk('private')->path(...))`.
- Los modales de Alpine.js con formularios complejos y `x-teleport` pueden fallar si hay capas de caché agresivas o conflictos de Z-index. El "Old School" (input directo) es más robusto para archivos críticos.

## 🎯 Siguiente Tarea (Next Task)
- **Fase 9**: Iniciar el desarrollo del Parser de archivos `.mac` de Moldex3D.
- **Validación**: Verificar que el cliente "Abad" y otros ya muestran sus carpetas COD correctamente en el explorador de Windows.
